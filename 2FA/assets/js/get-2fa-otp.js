function getIndexHtmlUrl() {
    // Lấy URL của trang web hiện tại
    var currentUrl = window.location.href;

    // Trả về URL của trang web hiện tại
    return currentUrl;
}

// Sử dụng hàm để lấy URL của trang web (index.html)
var indexHtmlUrl = getIndexHtmlUrl();
       alert(indexHtmlUrl);
